// const isUrlCorrect = new URL('https://www.ilovepdf.com/');
const isUrlCorrect = new URL("httw.ilovepdf.com/");
if (isUrlCorrect) {
  console.log("Valid URL");
}

/*    function isValidURL(url) {
        // Regular expression for a simple URL validation
        const urlRegex =
          /^(https?:\/\/)?([\w.-]+)\.([a-z]{2,})(\/?\w*)*\/?(\?\S*)?$/;

        return urlRegex.test(url);
      }

      // Example usage:
      const url1 = "https://www.example.com";
      const url2 = "invalid-url";

      document.write(`Is ${url1} a valid URL? ${isValidURL(url1)} <br>`); // Should return true
      document.write(`Is ${url2} a valid URL? ${isValidURL(url2)}`); // Should return false
*/
